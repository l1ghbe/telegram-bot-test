const startMessage = require('./startMessage')
const infoMessage = require('./infoMessage')
const currentDateAndTime = require('./date.js')
const textToBot = require('./textToBot.js')


module.exports = {
    infoMessage,
    startMessage,
    textToBot,
    currentDateAndTime
}